document.querySelectorAll('a[target="_blank"]').forEach(a=>{if(!a.textContent.includes('(opens in new tab)')){const s=document.createElement('span');s.className='small';s.textContent=' (opens in new tab)';a.appendChild(s)}});
// Load YouTube only after the visitor requests it.
document.querySelectorAll('.video-load').forEach(button=>{button.addEventListener('click',()=>{const holder=button.closest('.video-placeholder');const src=holder.dataset.videoSrc;if(!src)return;const wrap=document.createElement('div');wrap.className='video-wrap';const frame=document.createElement('iframe');frame.src=src;frame.title=holder.querySelector('strong')?.textContent||'Video playlist';frame.loading='lazy';frame.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';frame.allowFullscreen=true;wrap.appendChild(frame);holder.replaceWith(wrap);});});
